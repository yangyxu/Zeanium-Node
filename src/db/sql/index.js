zn.define([
    './Transaction',
    './TransactionBlock'
], function (Transaction, TransactionBlock) {

    return {
        Transaction: Transaction,
        TransactionBlock: TransactionBlock
    }

});
